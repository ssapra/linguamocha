<div class="main">
	<div class="person grid_12">
		<div class="name grid_4">	
			<figure>
				<figcaption><h1><%= @user.name %></h1></figcaption>
				<% if @user.avatar_file_name %>
					<%= image_tag @user.avatar.url(:medium) %>
				<% end %><br />
				<% if @user == current_user %>
					<%= link_to "Edit Profile", edit_user_path(@user.username), :class => "edit" %><br />
					<%= "You are near #{@location} because your ip is #{@ip}" %>
				<% else %>
					<%= link_to "Ask for Help", new_request_path(:sender_id => @current_user.id, :receiver_id => @user.id)%>
				<% end %>
			</figure>
		</div>

		<h2>Bio</h2>
		<% if @user.bio %>
			<%= @user.bio %><br /><br />
		<% else %>
			<i>No bio given</i>
		<% end %>

		<div class="info grid_8">
			<h2>General Information</h2>

			<div class="education grid_8">
				<div class="label grid_2">
					<p>Education:</p>
				</div>
               
                <% if @education != [nil, nil, nil] %>
					<div class="label_responses grid_6">
						<p><strong><%= @user.high_school%></strong><br>
							<strong><%= @user.college %></strong><br>
							<em><%= @user.degree %></em>
						</p>
					</div>
				<% else %>
					<i>No education listed</i>
				<% end %>
			<div/>

			<div class="occupation grid_8">
				<div class="label grid_2">
					<p>Occupation:</p> 
				</div>

				<div class="label_responses grid_6">
					<p><strong><%= @user.occupation %></strong></p>
				</div>
			</div>
		</div>
	
		<div class="skills grid_8">
			<h3>Skills</h3>
			<% if @user.my_skills != [] %>
				<% @user.my_skills.each do |skill| %>
				    <p><%= skill.tag %></p> 
				    <%= skill.description %>
				<% end %>
			<% else %>
				<i>No skills listed</i>
			<% end %>
		</div>
		
		<div class="learning grid_8">
			<h3>Currently Learning</h3>
			<% if @user.interests != [] %>
				<% @user.interests.each do |i| %>
					<p><%= i.tag%></p>
				<% end %>
			<% else %>
				<i>Not learning anything at the moment</i>
			<% end %>
		</div>
	</div>
</div>


<div class="reference_group grid_12">
	<h4>References</h4>

	<div class="reference grid_12">
		<div class="reviewer grid_2">
			<a href="#"><h5>Ben Siegel</h5></a>
			<img src="../img/ben.jpg" alt="Ben Siegel">
		</div>

		<div class="review grid_10">
			<p>I had a good time. We just sat around in his room late at night and watched Lost. It was fairly romantic, actually.</p>
		</div>
	</div>

	<div class="reference grid_12">
		<div class="reviewer grid_2">
			<a href="#"><h5>Victor Kung</h5></a>
			<img src="../img/victor.jpg" alt="Victor Kung">
		</div>

		<div class="review grid_10">
			<p>4.5 out of 5 stars. Would do again.</p>
		</div>
	</div>
</div>
	